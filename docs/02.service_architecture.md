┌────────────────────────────────────────────────────────┐
│                     Browser                            │
│--------------------------------------------------------│
│ Next.js (App Router)                                   │
│                                                        │
│ [지역 상태]                                             │
│ - 입력값, 버튼 상태, 로딩, 모달                          │
│                                                        │
│ [전역 상태 - Zustand/RTK]                               │
│ - 인증 상태                                             │
│ - 대기열 상태 (WAITING / ALLOWED)                       │
│ - 구매 단계 (SEAT_SELECT / PAYMENT)                     │
│                                                        │
│ [서버 상태 - React Query]                               │
│ - 좌석 목록 조회 (캐시 5초) - 좌석 지정만                 │
│ - 잔여 수량 조회 (캐시 3초) - 좌석 미지정만               │
│                                                        │
│ [실시간 통신 - SSE/WebSocket]                           │
│ - 대기열 순번 업데이트 (polling 대체) ✨                 │
│ - 좌석 상태 실시간 반영 (좌석 지정만)                     │
│                                                         │
│ 역할                                                    │
│ - 사용자 인터랙션                                        │
│ - Optimistic UI 업데이트                                │
│ - 실패 UX (대기, 차단, 재시도)                           │
└────────────────────────────────────────────────────────┘
                │
                │ HTTP/WSS 요청
                ▼
┌────────────────────────────────────────────────────────┐
│                       Edge                             │
│--------------------------------------------------------│
│ Cloudflare Workers / CloudFront Functions              │
│                                                        │
│ [검증 레이어] ✨                                       │
│ - JWT 인증 토큰 검증                                    │
│ - 대기열 토큰 검증 (Redis Lua Script)                   │
│ - Rate Limit (IP 기반, 1초 10req)                      │
│                                                        │
│ [캐시 레이어]                                           │
│ - 공연 목록 (CDN 캐시)                                  │
│ - 정적 자산                                             │
│                                                        │
│ 역할                                                   │
│ - 악의적 트래픽 조기 차단 (99%)                          │
│ - 대기열 미통과자 리다이렉트                             │
│ - 검증된 요청만 Origin 전달                             │
│                                                        │
│ 결과                                                   │
│ - 대기열 토큰 없음 → 대기 페이지                         │
│ - 토큰 만료/위조 → 403                                  │
│ - 정상 → X-Queue-Token 헤더 추가 후 통과 ✨             │
└────────────────────────────────────────────────────────┘
                │
                │ 검증된 요청 (X-Queue-Token 포함)
                ▼
┌────────────────────────────────────────────────────────┐
│                   Load Balancer                        │
│--------------------------------------------------------│
│ AWS ALB / Nginx (간소화) ✨                            │
│                                                        │
│ 역할                                                    │
│ - Round Robin 분산                                      │
│ - Health Check                                         │
│ - TLS Termination                                      │
│                                                        │
│ 특징                                                   │
│ - Rate Limit 제거 (Edge에서 처리) ✨                   │
│ - 비즈니스 로직 없음                                    │
└────────────────────────────────────────────────────────┘
                │
                │ 내부 요청
                ▼
┌────────────────────────────────────────────────────────┐
│                   Express.js API                       │
│--------------------------------------------------------│
│                                                        │
│ [미들웨어]                                              │
│ - X-Queue-Token 헤더 검증만 (Redis 조회 없음) ✨        │
│ - 인증 미들웨어 (JWT 재검증 선택적)                      │
│                                                        │
│ ════════════════════════════════════════════════════   │
│ 🎫 좌석 미지정 구매 (선착순)                            │
│ ════════════════════════════════════════════════════   │
│                                                        │
│ GET /shows/:id/available-count                         │
│  └─ Redis: GET stock:{showId} ✨                      │
│     (Redis가 단일 진실 소스)                            │
│                                                        │
│ POST /purchase/non-reserved  ✨                        │
│  ├─ Redis 원자적 연산 (Lua Script)                      │
│  │   ├─ WATCH stock:{showId}                           │
│  │   ├─ GET stock:{showId}                             │
│  │   ├─ IF stock > 0 THEN                              │
│  │   │    DECR stock:{showId}                          │
│  │   │    SADD purchased:{showId} {userId}             │
│  │   │    RETURN purchaseId                            │
│  │   └─ ELSE RETURN nil                                │
│  │                                                     │
│  ├─ 성공 시 비동기 DB 기록 (이벤트 큐) ✨                │
│  │   └─ Queue.publish('purchase.created', data)        │
│  │                                                     │
│  ├─ 결제 API 호출 (외부)                                │
│  │                                                     │
│  └─ 결제 실패 시                                        │
│      ├─ Redis: INCR stock:{showId} (복구)              │
│      └─ Redis: SREM purchased:{showId} {userId}        │
│                                                        │
│ ⚠️  실패 처리                                          │
│  - Redis 장애 시 → 503 응답 (구매 중단) ✨              │
│  - 재고 없음 → 404 "매진"                               │
│                                                        │
│ ════════════════════════════════════════════════════   │
│ 🪑 좌석 지정 구매                                       │
│ ════════════════════════════════════════════════════   │
│                                                        │
│ GET /seats/:showId                                     │
│  └─ DB: SELECT * WHERE showId = ? ✨                   │
│     (DB가 단일 진실 소스)                               │
│                                                        │
│ POST /seats/hold  ✨                                   │
│  ├─ Redis: 중복 HOLD 방지만 (보조 역할)                  │
│  │   ├─ EXISTS hold:{userId}:{showId}                  │
│  │   └─ SETEX hold:{userId}:{seatId} 300 "temp"        │
│  │                                                     │
│  ├─ DB 트랜잭션 시작 ✨                                 │
│  │   ├─ BEGIN                                          │
│  │   ├─ SELECT * FROM seats                            │
│  │   │   WHERE id = ? FOR UPDATE                       │
│  │   ├─ IF status = 'AVAILABLE' THEN                   │
│  │   │    UPDATE status = 'HOLDING'                    │
│  │   │    UPDATE holdBy = userId                       │
│  │   │    UPDATE holdUntil = NOW() + 5분               │
│  │   └─ COMMIT                                         │
│  │                                                     │
│  └─ Response: holdToken (DB의 holdUntil 기준)           │
│                                                        │
│ POST /purchase/reserved  ✨                            │
│  ├─ holdToken 검증                                      │
│  │                                                     │
│  ├─ DB 트랜잭션 시작 ✨                                 │
│  │   ├─ BEGIN                                          │
│  │   ├─ SELECT FOR UPDATE                              │
│  │   ├─ 상태 검증 (HOLDING + holdBy 일치)               │
│  │   ├─ UPDATE status = 'SOLD'                         │
│  │   ├─ INSERT INTO purchases                          │
│  │   └─ COMMIT                                         │
│  │                                                     │
│  ├─ 결제 API 호출 (외부)                                │
│  │                                                     │
│  ├─ 결제 성공 → Redis hold 정리                         │
│  │   └─ DEL hold:{userId}:{seatId}                     │
│  │                                                     │
│  └─ 결제 실패 시                                        │
│      └─ DB: UPDATE status = 'AVAILABLE' (롤백)         │
│                                                        │
│ ⚠️  실패 처리                                          │
│  - DB 장애 시 → 503 응답 (구매 중단) ✨                 │
│  - 좌석 선점됨 → 409 "이미 선택된 좌석"                  │
│                                                        │
│ ════════════════════════════════════════════════════   │
│                                                        │
│ [백그라운드 워커] ✨                                    │
│ - HOLDING 상태 만료 정리 (5분마다)                       │
│   UPDATE seats SET status = 'AVAILABLE'                │
│   WHERE status = 'HOLDING'                             │
│   AND holdUntil < NOW()                                │
│                                                        │
│ - Redis → DB 동기화 (좌석 미지정, 10초마다)              │
│   이벤트 큐에서 구매 기록 일괄 처리                       │
│                                                        │
└────────────────────────────────────────────────────────┘
          │                           │
          │ 캐시/카운터              │ 트랜잭션
          ▼                           ▼
┌───────────────────────┐   ┌──────────────────────────┐
│        Redis          │   │    PostgreSQL/MySQL      │
│-----------------------│   │--------------------------│
│                       │   │                          │
│ ══ 공통 ══            │   │ ══ 공통 ══                │
│ queue:{token}         │   │ shows                    │
│  - 대기열 관리         │   │  - id, title, date       │
│                       │   │  - seatType (reserved/   │
│ ══ 좌석 미지정 ══ ✨   │   │   non_reserved) ✨      │
│ stock:{showId}        │   │                          │
│  - 남은 티켓 수        │   │ ══ 좌석 미지정 ══ ✨     │
│  - 🎯 단일 진실 소스   │   │ purchases_bulk          │
│                       │   │  - Redis에서 비동기 적재  │
│ purchased:{showId}    │   │  - 감사/정산용만          │
│  - Set (중복 방지)     │   │                         │
│                       │   │ ⚠️  DB는 보조 역할       │
│ ⚠️  장애 시 서비스     │   │                         │
│     중단 필요          │   │ ══ 좌석 지정 ══ ✨       │
│                       │   │ seats                    │
│ ══ 좌석 지정 ══ ✨    │   │  - id, showId, row       │
│ hold:{user}:{seat}    │   │  - status (AVAILABLE,    │
│  - 중복 방지용         │   │    HOLDING, SOLD) ✨    │
│  - TTL 300s           │   │  - holdBy, holdUntil     │
│  - 보조 역할만         │   │  - 🎯 단일 진실 소스     │
│                       │   │                          │
│ cache:seats:{showId}  │   │ purchases                │
│  - DB 캐시            │   │  - userId, seatId        │
│  - TTL 5s             │   │  - amount, paidAt        │
│                       │   │                          │
│ ⚠️  장애 시 느려짐     │   │ ⚠️  장애 시 서비스       │
│     (DB로 대체)        │   │     중단 필요            │
└───────────────────────┘   └──────────────────────────┘
